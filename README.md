# Barcode Playground

Static GitHub Pages demo for [`html5-qrcode`](https://github.com/mebjas/html5-qrcode).

## Local use

Open [`index.html`](./index.html) in a browser, or serve the directory with a simple static server.

## GitHub Pages

Publish the repository from the default branch root. The site has no build step.
